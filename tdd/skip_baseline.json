[
  {
    "reason_prefix": "e2e-lane: ",
    "note": "Tests that need a live backend URL reachable from the container network (workspace-population real-http clone). Gated on ENV PRESENCE (E2E_BACKEND_URL/BACKEND_URL), never availability: env set + backend unreachable = loud failure. They execute in the compose e2e lane where the env is set; skipping there is out-of-baseline by definition (reason changes when env is set - it cannot skip)."
  },
  {
    "reason_prefix": "Legacy pipeline format removed",
    "note": "tdd/e2e/test_graph_pipeline.py legacy-conversion tests kept as tombstones for the removed format; delete the tests and this entry together."
  }
]
