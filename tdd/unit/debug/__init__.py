"""Debug-terminal client/server contract tests (Phase 12.7).

Carries an ``__init__.py`` like every other ``tdd/unit/*`` subpackage: a test
directory without one collects inconsistently once two files in the tree
share a module basename.
"""
